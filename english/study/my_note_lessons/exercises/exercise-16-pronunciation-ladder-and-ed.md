# Exercise 16 — Pronunciation ladder and `-ed`

1. Read: `bow–bound–bout`, `fine–find–fight`, `pan–panned–pant`.
2. Sort into `/t/`, `/d/`, `/ɪd/`: `walked, played, wanted, washed, called, decided, needed, loved`.
3. Write one sentence using `wait for`, one using `listen to`, and one using `look for`.
4. Correct: `While I was waiting, I was listenning to music.`

